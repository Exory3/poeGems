import {tv} from 'tailwind-variants/lite'

export const gemDetailsInfo = tv({
  base: 'text-3xl text-center my-10',
})
export const gemDetailsContainer = tv({
  base: ' grid w-8/10 m-auto mt-10',
})
export const gemDetailsBody = tv({
  base: ' grid md:grid-cols-3 gap-2 ',
})
