import {tv} from 'tailwind-variants/lite'

export const corruptionPagePrice = tv({
  base: 'ml-2 flex',
})

export const corruptionPagePriceIcon = tv({
  base: 'h-6 w-6',
})
export const corruptionPageItem = tv({
  base: 'grid grid-cols-4 gap-x-2',
})
