import {tv} from 'tailwind-variants/lite'

export const tableStyle = tv({
  base: 'table-fixed w-full border-collapse',
})
export const priceStyle = tv({
  base: 'text-left pl-4',
})

export const contributionStyle = tv({
  base: 'text-right pr-2',
})
