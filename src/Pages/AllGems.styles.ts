import {tv} from 'tailwind-variants/lite'

export const allGemsContainer = tv({
  base: ' grid max-w-8/10 m-auto',
})
export const allGemsInfo = tv({
  base: 'text-center text-2xl',
})
export const allGemsBody = tv({
  base: ' grid md:grid-cols-3 gap-x-3 gap-y-1 ',
})
